

  export function getCurrentLang() {
      /*let res = window.location.pathname.substring(1,3)
      if(!res){
          return "en"
      }
        return res
      */

    // As of now, only french is managed by the application.
    return "fr"
  }


